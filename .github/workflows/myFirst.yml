name: myfirstworkflow

on:
  push:
    branches:
        - main
    paths:
        - "**.html"
        - "**.txt"

jobs:
  sayMyName:
    runs-on: ubuntu-latest
    steps:
      - name: call my name
        run: echo "my name is Kaushik!!"
      - name: print the runner name
        run: echo "name of runner is $USER"
